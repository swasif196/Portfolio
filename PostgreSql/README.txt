Sohaib Wasif

All scripts run to the sepcs identified in the assignment. For 8th script when you run the function it ouputs a table which consists of the transactions record added, customer name and balance, and vendor name and balance. If you enter invalid Vno or Account then a notice appears indicating you entered invalid data and an empty table is printed. Each script is named p1, p2, p3, p4...p8. The function names in each script is the same as the script name.

p1.sql -> p1(parameters)
p2.sql -> p2(parameters)
        .
        .
        .
        .
p8.sql <- p8(parameters)